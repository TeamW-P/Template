from services.Service import *

# TODO rename template to the name of the tool
class Template:

    @staticmethod
    def template_endpoint(arguments):
        '''
        This is where the core logic of the new tool goes. Add as many new methods 
        and files as needed. These methods will be called at the route layer.
        '''
        return arguments